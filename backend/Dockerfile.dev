# ===== 開發環境 Dockerfile =====
# 此檔案專用於開發環境，支援熱重載和開發工具
FROM node:22-alpine

RUN apk add --no-cache curl

WORKDIR /app

COPY package*.json ./
RUN npm install
RUN npm install -g @nestjs/cli nodemon

COPY . .

EXPOSE 3000
# 開發環境啟動指令 (支援熱重載)
CMD ["npm", "run", "start:dev"]