// # productTypes-api to-do:

//   -  `Create product type` (admin's)
//       - **create a product type with admin role**
//       - **create a product type with parent type id and admin role**
//       - create a product type with non existent parent type id and admin role
//       - create a product type with non-permission role

//   - `Get product types` (public)
//       - **get all of product types**
//       - **get product types with filter**
      
//   - `Get product types` (admin's)
//       - **get all of product types with admin role**
//       - **get product types with filter with admin role**
//      - get product types with non-permission role

//   - `Get a single product type` (public)
//       - **get a single product type by id**
//       - get a single product type by non-existent product type id

//   - `Get a single product type` (admin's)
//       - **get a single product type by id**
//       - get a single product type by non-existent id
//       - get a single product type with non-permission role

//   - `Update product type data` (admin's)
//       - **update product type data by id**
//       - update product type with non-existent id
//       - update own data with non-permission role

//   - `Delete product type` (admin's)
//       - **delete product type by id**
//       - delete product type with non-existent id
//       - delete product type with non-permission role

//   - **Health test**